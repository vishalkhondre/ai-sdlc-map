# Review: Slate & Teal brand (edition 1.3.0, D-021)

## Scope

Three independent reviews by reviewer agents in fresh contexts (`.claude/agents/`), read-only. Round 1: `1c9a6a5..d7278d1` (the brand commit). The accuracy reviewer and the editorial and code reviewer returned REVISE; the fixes (`35baa5e`) were confirmed by all three (D-011), and `3811317` adds the Pi file to the fonts README table, as both confirmations suggested. The only reader-visible content change is one CHANGELOG 1.3.0 line; the change to every page is colour, type, logo and icons.

## Limitations

Screenshots are committed under `project/screenshots/1.3.0-brand/` because images cannot be attached to a pull request from this session. The OFL FAQ site was reachable by search only.

## Confidentiality reviewer

**Scope.** `git diff 1c9a6a5 d7278d1`: the changelog line, STATUS, `og_card()`, `test_brand.py`, `style.css`, `favicon.svg`, the logo in `generate.py`, 9 screenshots, 6 woff2 fonts and 2 licences.

**Evidence.** `check_citations.py` passed; 83 unit tests OK. Because the tracked-file scan skips binary files, the reviewer read every name record of the fonts and ran the deny-list matcher over them: no matches; the PNGs carry only IHDR, IDAT and IEND chunks. No D-019 match in the diff.

**Checks performed.** GR-1.1 names: pass. Resemblance to an employer's design system (D-021): no concern. A slate neutral scale with a deep teal primary and an amber accent is a common documentation pairing; token names are plain descriptive words; Atkinson Hyperlegible is a public accessibility typeface, and IBM Plex Mono used only for code suggests no vendor's design language; the logo is a generic geometric form; the social card has no motif pointing to any organisation. GR-1.2 to GR-1.4: pass. GR-3.3: the typefaces appear only as credits for bundled assets. D-015 and D-019: unaffected.

**Blocking issues.** None.

**Suggestions.** 1 Add an automatic check of font name tables and PNG metadata (GR-5.3). 2 On phones the five-bar logo sits beside the three-bar menu button. 3 The changelog names the typefaces as asset credits, which is fine.

Reviewer verdict: ACCEPT

### Confidentiality reviewer, confirmation (`d7278d1..35baa5e`)

`check_citations.py` passed; 84 unit tests OK, including the glyph-coverage test. The name tables of every woff2 file, the new licence and the fonts README: no deny-list matches. The three new files carry only IBM's copyright, the family name IBM Plex Mono, version 2.005 and vendor ID IBM. The retaken screenshot has no metadata chunks. The CSS changes are generic palette changes. GR-1.1 to GR-1.4, GR-3.3 and D-019: clean. Suggestion (GR-5.3): add a font name-table scan to the tracked-file deny-list test.

Reviewer verdict: ACCEPT

## Accuracy reviewer, round 1

**Evidence.** `check_citations.py`, `generate.py`, `render_diagrams.py` (og-image 1200x630), `check_pages.py`, the brand and page-text tests and `check_browser.py` pass. A browser probe loaded Atkinson Hyperlegible and IBM Plex Mono with no requests to other hosts; the logo and favicon have five bars in the recorded colours; the diagram sources and built copies are unchanged. Licences checked against IBM/plex `LICENSE.txt`, the google/fonts OFL files (direct reads) and the OFL FAQ (search only).

**Checks performed.** Every claim in the CHANGELOG line checked against the build: palette, light and dark, AA contrast, the typefaces, "served with the site", logo, favicon, social preview, unchanged band colours, unchanged page text: supported. GR-2.1 to GR-2.5: no claims, terms or references added.

**Blocking issues.** B1 (GR-2.4): the Plex licence header left out IBM's notice "Copyright © 2017 IBM Corp. with Reserved Font Name "Plex"". B2: the bundled Plex files were third-party subsets that keep the Reserved Font Name; bundle IBM's own unmodified files instead.

**Suggestions.** A fresh record is needed; amber also marks the "First" maturity; axe does not check text inside inline SVG.

Reviewer verdict: REVISE

### Accuracy reviewer, round 2 (`d7278d1..35baa5e`)

B1 fixed: `LICENSE-ibm-plex-mono.txt` is identical to `LICENSE.txt` in the `@ibm/plex-mono` 2.5.0 package. B2 fixed: the three Plex files are byte-identical to that package's `fonts/split/woff2/` files, IBM's own unmodified files, so keeping the name "Plex" is permitted; the "Plex Arrows" CSS alias only selects glyphs from the unchanged file. The CHANGELOG line is still accurate: 84 unit tests and `check_browser.py` pass, and the fonts load with no requests to other hosts. Suggestion: list the Pi file in the fonts README (done in `3811317`).

Reviewer verdict: ACCEPT

## Editorial and code reviewer, round 1

**Evidence.** `check_pages.py`, `generate.py`, `render_diagrams.py`, 83 unit tests, `check_browser.py` and `check_citations.py` pass. Probes: the bundled fonts' character maps against every non-ASCII character the site uses; a request log across all pages in both themes (no external requests); computed badge colours; axe with the workflow detail dialog open. All 9 screenshots viewed.

**Checks performed.** The 26 tokens match D-021 character by character, in both dark blocks; usage follows D-021 (links, primary buttons, navigation state, maturity, counterpart notes, workflow cells, logo, favicon); the diagram band colours are unchanged; fonts bundled with licences; the OG card renders; no provisional language (GR-3.2).

**Blocking issues.** B1 (GR-4.1): muted text on the agent cell was 4.16:1 in light, and the test had no such pair and axe never ran with the dialog open. B2: the bundled fonts lacked ← → ↔ ⤢. B3 (GR-2.2): the adopted and coined badges were identical.

**Suggestions.** Old theme-color, button and overlay colours; `--bg-3` turning neutral surfaces teal; parse D-021 name by name and drop rounding in the contrast test; wait on `document.fonts.ready` for the OG card; favicon PNG fallback; og:image size meta; STATUS wording; retake screenshots.

Reviewer verdict: REVISE

## Editorial and code reviewer, round 2 (`d7278d1..35baa5e`)

84 unit tests and `check_browser.py` pass; axe now also runs with the detail dialog open. B1 fixed: `--bg-3` is a neutral tint and the test models it, with the pairs the site draws on it and on the accent tint, no rounding, and D-021 parsed name by name. B2 fixed: ← → ↔ come from IBM's Plex Pi file; ⤢ is hidden and drawn as a CSS mask icon; a test asserts every non-ASCII character is in a bundled font. B3 fixed: coined is an outline (surface, ink, primary ring at 6.09:1). theme-color, button and overlay colours and the OG font wait confirmed. Not taken, accepted as the author's choice: a separate colour for "First", a PNG favicon, og:image size meta. Suggestion: list the Pi file in the fonts README (done in `3811317`).

Reviewer verdict: ACCEPT

## Result

Confidentiality ACCEPT, accuracy ACCEPT (round 2), editorial and code ACCEPT (round 2). No open blocking findings.

Verdict: ACCEPT
