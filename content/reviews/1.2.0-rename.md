# Review: The AI SDLC Map rename and the AI SDLC glossary entry (edition 1.2.0)

## Scope

Three independent reviews of `git diff 2a49b35 28d5982` (the rename, D-013, the glossary entry `ai-sdlc` and the references `ibm-ai-lifecycle`, `ibm-ai-dlc` and `aws-ai-dlc`), each by a reviewer agent in a fresh context, read-only, following `project/AGENTS.md`: confidentiality, accuracy and editorial. The editorial reviewer returned REVISE in round 1; the fixes in `git diff 28d5982 481f9e5` were confirmed in round 2 (D-011: a confirmation review of a diff relies on the round-1 reviews for everything outside it). After round 2, one cosmetic change to `site/generate.py` suggested by the editorial reviewer was applied: a duplicated author's source link reads "IBM — <title>" instead of "IBM: <title>". It changes no file under `content/`.

Source fingerprint at acceptance: `35960d52bb1ce9a21e4ca195a786a9e89f4ef8f942620c46e84d320690eb7002`.

## Limitation

The review sessions' network policy blocks direct fetches of ibm.com and builder.aws.com. The three new references were checked through web search result titles, URLs and content summaries, not a full read of each page (see the accuracy review). The accuracy reviewer's suggestions S-1 (the AWS post's individual byline) and S-2 (the word "drift" on the IBM page) are for a later check from a session that can open the pages.

## Confidentiality reviewer

**Scope.** The diff `origin/main` (2a49b35) to `28d5982` covers 18 files. It renames the site in README, CLAUDE.md, LICENSE, `.github` instructions and prompts, GOAL, DECISIONS (D-010 note, new D-013), `toc.yml` (title, tagline, new `about`), the map title in `build_map.py` and its SVG, `generate.py` (hero, page title, catalog lede, glossary source labels), `style.css` and one test string. The content changes are edition 1.2.0, a CHANGELOG entry, a new glossary entry `ai-sdlc`, and three new references (`ibm-ai-lifecycle`, `ibm-ai-dlc`, `aws-ai-dlc`). The built site at `site/*.html` was also checked, as a public reader would see it.

**Evidence.** HEAD `28d5982d08a0ecdacff0e07fb0e6d0e2fa5dc731`; base `origin/main` `2a49b35faa6a6865111baf68b86b419489531542`; clean tree. `python scripts/check_citations.py` passed ("0 chapters, 7 series parts, 12 citations, 35 terms, 14 references"). `python -m unittest discover -s scripts/tests`: 42 tests, OK. Read the full diff and the rendered text of `glossary.html`, `index.html`, `references.html`, `404.html` and `workflow-catalog.html`. Searched the diff and the built HTML privately for the protected names: no matches (the terms are not repeated here, GR-1.4). Neither `BANNED_IN_PROSE` nor `PRODUCT_NAMES` in `check_citations.py` contains AWS or IBM, so no automated check covers these two names.

**Checks performed.**
- GR-1.1: every added line is a public product or site name, a public vendor (IBM, AWS), a public URL, or the author's own public name and link (already present). No employer, group, product, platform, project, team or people names, internal codes, customers or internal figures. Pass.
- GR-1.2: the glossary definition and the reference notes read as general industry terminology, summarising public IBM Think and AWS Builder Center pages in the site's own words. Pass.
- GR-1.3: no new documents, extracts or research notes; only tracked source and config files changed. Pass.
- GR-1.4: `scripts/denylist.py` untouched; D-013 names no protected term. Pass.
- GR-3.3: IBM and AWS appear only in the glossary entry, the three references and the CHANGELOG and DECISIONS text, each time credited as the source or originator of a term, not recommended as a tool (the attribution GR-2.2 requires). AI-DLC is described as "one vendor's named methodology", and the entry says the site "does not follow any one methodology". The glossary is the terminology page and the entry defines a term, so this is a fair reading of "a page about its category". No vendor name in the map diagram. The author asked for these citations explicitly. Acceptable.

**Blocking issues.** None.

**Suggestions (non-blocking).**
1. GR-3.3: every page embeds the glossary as JSON for term tooltips; if `match: ["AI SDLC"]` ever fires on reader-visible text on other pages, the tooltip would carry the AWS and IBM wording. Consider showing only the first sentence in tooltips.
2. GR-3.3 / GR-5.3: AWS and IBM are on no automated list; consider an allow-list saying where vendor originators may be named (glossary and references only).
3. GR-3.3: a neutral third-party description of AI-DLC besides IBM's would further dilute the vendor weight. Optional.

Reviewer verdict: ACCEPT

## Accuracy reviewer

**Scope.** `git diff origin/main 28d5982` (18 files): the new glossary entry `ai-sdlc` (attribution `common`, `also: [ibm-ai-lifecycle, ibm-ai-dlc, aws-ai-dlc]`), the three new references, CHANGELOG 1.2.0, D-013 and the D-010 note, the rename text in `toc.yml` and `README.md`, and the built `glossary.html`, `references.html` and `index.html`. Read-only.

**Evidence.** Commit `28d5982d08a0ecdacff0e07fb0e6d0e2fa5dc731`. `python scripts/check_citations.py` printed "Citation check passed: 0 chapters, 7 series parts, 12 citations, 35 terms, 14 references." and exited 0. The network policy of the review session blocks direct fetches of ibm.com and builder.aws.com, so no source page was opened directly. Everything below comes from web search result titles, URLs and the search tool's content summaries (seven searches, some limited to ibm.com). Verified: each URL exists and is indexed, each page title, and the page content as summarised. Not verified: the full page text; the individual author of the AWS Builder Center post; with certainty, that "drift" appears on the IBM AI lifecycle page itself (S-2).

**Checks performed.**

| # | Claim | Source | Supported? |
|---|---|---|---|
| 1 | AI SDLC = the SDLC with AI agents doing part of the work in every phase, checks and human decisions kept where needed; written without a hyphen | The site's own usage (D-013) | Yes: a statement of usage, not a finding (GR-2.3); `common` suits it, as other public pages use the same wording (GR-2.2) |
| 2 | The lifecycle for building AI/ML systems (data preparation, training and evaluation, deployment, monitoring for drift), "which IBM calls the AI lifecycle" | `ibm-ai-lifecycle` | Yes: "an iterative process of planning, developing, deploying and maintaining AI systems, from dataset preparation to model training to monitoring and improvement"; drift, see S-2 (GR-2.4) |
| 3 | `ibm-ai-lifecycle` note | `ibm-ai-lifecycle` | Yes, matches the page's summary sentence (GR-2.4) |
| 4 | `ibm-ai-lifecycle` title, org, URL | Search result "What Is the AI Lifecycle? \| IBM" at that URL | Yes (GR-2.5) |
| 5 | AI-DLC is "one vendor's named methodology" | `ibm-ai-dlc`, `aws-ai-dlc` | Yes: IBM says it "was coined by AWS's Raja SP in 2025"; AWS calls it "a new methodology" (GR-2.4) |
| 6 | `ibm-ai-dlc` note (AI as collaborator in every step; credited to AWS, 2025) | `ibm-ai-dlc` | Yes (GR-2.4) |
| 7 | `ibm-ai-dlc` title, org, URL | Search result "AI-DLC: The AI-Driven Development Lifecycle \| IBM" at that URL | Yes (GR-2.5) |
| 8 | "AWS structures it as Inception, Construction and Operations phases" | `aws-ai-dlc` | Yes: "AWS structures AI-DLC as three phases executed in order: Inception → Construction → Operations" (GR-2.4) |
| 9 | AI proposes, asks clarifying questions, people approve; "a loop" | `aws-ai-dlc` | Yes: Plan / Clarify / Approve, and "AI-DLC is a loop" (GR-2.4) |
| 10 | `aws-ai-dlc` title and URL | Search result "SDLC Who? Meet AI-DLC: The New Build Loop \| AWS Builder Center" at that URL | Yes (GR-2.5); author field, S-1 |
| 11 | Access dates on all three references | `accessed: "2026-09-25"` | Yes (GR-2.5) |
| 12 | CHANGELOG 1.2.0 and D-013 | The entries above | Consistent, no extra factual claims (GR-2.3) |
| 13 | No invented statistics, quotes or case studies | Whole diff | None found (GR-2.3) |

**Blocking issues.** None.

**Suggestions (non-blocking).**
- S-1 (GR-2.5): `aws-ai-dlc` names the platform as author; when the page can be opened, record the individual byline and keep AWS / AWS Builder Center as `org`.
- S-2 (GR-2.4): "monitoring for drift" is supported in substance (IBM Think describes model drift as a reason for monitoring and retraining), but the search summary mixed text from several IBM pages. If a direct read of the AI lifecycle page does not mention drift, reword to "monitoring and improvement".
- S-3 (GR-2.1, optional): IBM credits the method to an AWS whitepaper, "AI-Driven Development Lifecycle (AI-DLC) Method Definition" (2025); if public, it is a more primary source.
- S-4 (GR-2.4, optional): consider "(credited by IBM to AWS, 2025)" in the definition itself.

Reviewer verdict: ACCEPT

## Editorial reviewer, round 1

**Scope.** `git diff origin/main 28d5982` and the built output: index, glossary, references, workflow-catalog and 404 pages, `llms.txt`, `llms-full.txt`, `og-image.png`, and the map PNG and SVG. Read-only.

**Evidence.** No "AI SDLC on one page", "The AI SDLC on one page" or "AI-SDLC" remains in `site/`, `content/` or `README.md`, apart from the historical review record `content/reviews/1.0.0-seed-confirmation.md`. Page titles: "The AI SDLC Map — the AI-assisted software lifecycle on one page" (home), "Terminology and sources · The AI SDLC Map", "References · The AI SDLC Map", "SDLC workflow catalog · The AI SDLC Map", "Not found · The AI SDLC Map". og:title matches the home title; og:description and the meta description use the new `about` text. `og-image.png` is byte-identical to the map PNG, whose heading reads "The AI SDLC Map". The map SVG `<title>` and heading are "The AI SDLC Map". Screenshots of the home page at 1280 px and 390 px: no overflow.

**Checks performed.** Brief coverage (title, subtitle, home, page titles, social preview, README, map `<title>`, no-hyphen term, glossary entry with both notes, 1.2.0 and changelog): met, apart from B1. Spell-out at first use (D-013): brand text in the nav, `<title>`, breadcrumbs, diagram titles, glossary term chips, series part titles and a page's own H1 were treated as names; anything in running prose as a use. Home page, workflow catalog, glossary and `llms-full.txt` pass; `llms.txt` and 404 have no body use; the References page fails (B1). GR-3.1 and GR-3.2: pass; the banned-phrase scan finds only existing legitimate uses ("first drafts", the search input's `placeholder` attribute). GR-4.1: the entry and the three references are complete. GR-3.3: IBM and AWS named only in the disambiguation entry and its references, as D-013 authorises. Changelog: accurate, apart from S4. The entry's wording matches the README, `toc.yml` `about`, the home hero and D-013.

**Blocking issues.**
- B1 (D-013, GR-4.2): the `ibm-ai-lifecycle` note used "AI SDLC" in running prose on the References page without spelling it out, and read cryptically out of context.

**Suggestions.** S1 (D-013): the catalog H1 "SDLC workflow catalog" precedes the spelled-out lede; accepted as the page name, for the author to confirm. S2 (GR-3.1): "in which AI proposes, asks clarifying questions and people approve" reads awkwardly. S3 (GR-4.2): source links "What Is the AI Lifecycle?" and "AI-DLC: …" do not show they are IBM's. S4: the changelog does not mention the source-label change. S5 (GR-4.1): the glossary summary table does not show the "common" group; pre-existing. S6: move "Written without a hyphen." to the end.

Reviewer verdict: REVISE

## Editorial reviewer, round 2 (confirmation of `git diff 28d5982 481f9e5`)

**Checks.**
- B1 resolved: the `ibm-ai-lifecycle` note now reads "…The terminology page separates this lifecycle for building AI systems from the AI-assisted software development lifecycle that this site describes." On the rebuilt References page, "AI SDLC" appears only in term chips and "The AI SDLC Map" only in the breadcrumb and diagram lists (names); "SDLC" otherwise appears only inside a cited source's title.
- S2 taken: "in which AI proposes a plan and asks clarifying questions, and people approve", with the matching comma in the `aws-ai-dlc` note. The reworded phrase agrees with the `aws-ai-dlc` note and the published account of AI-DLC (AI plans, asks clarifying questions, and proceeds after a person approves); "a plan" adds nothing the source does not say. The AWS page was not reopened in this round.
- S3 taken: source links render "IBM: What Is the AI Lifecycle?", "IBM: AI-DLC: The AI-Driven Development Lifecycle" and "AWS Builder Center"; the conditional behaves as intended for authors cited once, twice, or missing.
- S4 taken: the changelog clause describes the change accurately. S6 taken.
- GR-3.2 and GR-3.3: no new issues. S1 accepted pending the author's ruling; S5 deferred, pre-existing.

**Blocking issues.** None.

**Suggestions.** Cosmetic (GR-3.1): "IBM: AI-DLC: …" has two colons in a row; use another separator, such as "IBM — AI-DLC: …". A one-character change in `generate.py` with no changelog entry needed. (Applied after this review; see Scope.)

Reviewer verdict: ACCEPT

## Result

Confidentiality ACCEPT, accuracy ACCEPT, editorial ACCEPT (round 2). No open blocking findings.

Verdict: ACCEPT
