# Review: documentation layout (edition 1.3.0, D-020)

## Scope

Three independent reviews by reviewer agents in fresh contexts (`.claude/agents/`), read-only. Round 1: `922a9e2..f4fde72` (the page-text snapshot, then the layout). The editorial and code reviewer returned REVISE; the accuracy reviewer's suggestions (`2e6a44d`) and the editorial fixes (`4348759`) were confirmed by all three (D-011). The only reader-visible content change is one CHANGELOG 1.3.0 line; the change to every page is layout and navigation. Source fingerprint at acceptance: `a36623d760fe5ab57c5aa42ada96842c9f9c828c859ce9455ef7ca8b526c3c1c`.

## Limitations

Screenshots are committed under `project/screenshots/1.3.0-layout/` because images cannot be attached to a pull request from this session.

## Confidentiality reviewer

**Scope.** `git diff 922a9e2 f4fde72` (the page-text snapshot, then the layout; 20 files including 8 screenshots) and both commit messages. Read-only.

**Evidence.** `python scripts/check_citations.py`: "Citation check passed: 0 chapters, 0 citations, 35 terms, 14 references." `python -m unittest discover -s scripts/tests`: 77 tests OK. `python scripts/check_pages.py`: passed. `denylist.matches()` run by hand over every changed text file, every built page and both commit messages: 18 files, 0 hits. Searches for the series address, repository and title: none; only internal notes saying "earlier (narrative) series (D-019)". All 8 screenshots viewed: only the rendered public site, no browser chrome, user names, paths or notifications, and no metadata text blocks.

**Checks performed.** GR-1.1 names: pass (new visible text is generic navigation wording). GR-1.1 fingerprint: pass (a three-column documentation layout is a common pattern). GR-1.2: pass (text unchanged, proven by the snapshot test). GR-1.3: pass (two scripts, a snapshot of the site's own published text, screenshots of the public site). GR-1.4: pass. GR-3.3: pass (the accessibility tool is named only in test tooling). D-019: pass. D-015: untouched.

**Blocking issues.** None.

**Suggestions.** 1 The automatic deny-list scans only `content/`; consider extending it to all tracked files and commit messages with the keyed deny-list work (D-023). 2 Keep project wording generic, as now. 3 The brand work needs its own check that it resembles no employer's design system.

Reviewer verdict: ACCEPT

### Confidentiality reviewer, confirmation (`f4fde72..4348759`)

`check_citations.py` passed; 79 unit tests OK; the deny-list matcher over the changed files, built pages and commit messages: 0 hits; no link to or name of the earlier series in the snapshots, pages, changelog or commits. All 8 retaken screenshots show only the public site, with no metadata text. New visible words are generic ("Reference", "Workflows", "Breadcrumb"). GR-1.1 to GR-1.4, GR-3.3 and D-019: clean. Suggestion: rely on CI to run `check_browser.py` for the rendered-catalog comparison.

Reviewer verdict: ACCEPT

## Accuracy reviewer

**Scope.** `922a9e2..f4fde72`; the one reader-visible content change is a CHANGELOG 1.3.0 line; the built pages. Read-only.

**Evidence.** `check_citations.py` passed; `test_page_text.py` 2 tests OK; all 77 unit tests OK; `check_pages.py` passed; `site/generate.py` rebuilds with no diff. `release_content.py check` fails as stale, as expected before a new record. `check_browser.py` could not run in the review session (Chromium download blocked). An independent comparison built the old site from `922a9e2`: old build = snapshot = new build for all 5 pages; a stricter comparison keeping breadcrumbs also matched; `data-chrome` marks only the previous/next and section-chooser elements; titles, meta, OG tags, footer and every non-HTML output are identical apart from `app.js` and `style.css`.

**Checks performed.** Each feature named in the changelog line checked against the built site: supported, except that "On this page" does not appear on the catalog (no sections) and the keyboard and screen-reader claim was not verifiable in the review session. New navigation labels accurate. GR-2.1 to GR-2.5: no claims, terms or references added.

**Blocking issues.** None.

**Suggestions.** 1 Say "On this page" appears on pages with sections. 2 Soften the accessibility claim to what is tested, and confirm `check_browser.py` passes before recording. 3 The 404 page's left column is empty; show the sections. 4 Glossary terms appear nested under "Translation table" in On this page. 5 Note in `page_text.py` that the guard ignores heading levels. 6 A fresh record is needed for 1.3.0.

Reviewer verdict: ACCEPT

### Accuracy reviewer, confirmation (`f4fde72..4348759`)

The reworded changelog line checked claim by claim against the rebuilt site: accurate ("On this page" on pages with sections; breadcrumbs that name the section; 404 shows the sections; References entries are h2; the only new words are navigation labels). The new extractor run over an independent build of `922a9e2` equals the new snapshot on all 5 pages, so the snapshot was not re-baselined over a text change. `check_browser.py` passes, settling the accessibility claim.

Reviewer verdict: ACCEPT

## Editorial and code reviewer, round 1

**Evidence.** `site/generate.py`, `check_pages.py`, 77 unit tests and `check_browser.py` (axe on every page, both themes, two widths) pass. The snapshot equals a build of `922a9e2` taken before the layout. A mutation (a workflow name changed) still passed the text test. Tab order, reduced motion and every "On this page" link checked.

**Checks.** The brief is met: tabs, left navigation, "On this page", previous and next, the section chooser, drawer, skip link, labelled landmarks, `aria-current`, visible focus, reduced motion, no framework, screenshots.

**Blocking issues.** B1 (D-020, GR-4.1): the text test did not cover the catalog, whose workflows are drawn by JavaScript from a JSON block. B2 (accessibility): with the drawer open, focus could move to page links behind the scrim.

**Suggestions.** Allowlist the chrome markers and include text alternatives; glossary terms nested under "Translation table"; breadcrumbs without the section; changelog accuracy; empty 404 column; full-length home screenshots; wider browser checks; `_headings` should skip scripts.

Reviewer verdict: REVISE

## Editorial and code reviewer, round 2 (`f4fde72..4348759`)

79 unit tests and `check_browser.py` pass. B1 fixed: the extractor includes every JSON data block and text alternatives, chrome markers are allowlisted, and a rendered-catalog snapshot is compared in the browser; both snapshots equal a build of `922a9e2`; the round-1 mutation and a new one (marking the catalog heading as chrome) both fail the test. B2 fixed: the page behind the drawer is inert, and the browser test asserts focus stays in the drawer or header. Suggestions 1–7 and 9 applied.

Reviewer verdict: ACCEPT

## Result

Confidentiality ACCEPT, accuracy ACCEPT, editorial and code ACCEPT (round 2). No open blocking findings.

Verdict: ACCEPT
