# Review: wave 0 remainder (edition 1.2.1)

## Scope

Three independent reviews, each by a reviewer agent in a fresh context running from its definition in `.claude/agents/` and its skill, read-only. Round 1 covered `git diff 6762b1d f98a1dc` (reviewed as `origin/main..4f11991` before a rebase that changed nothing in this work): the access-date and outdated-terms checks, the six page templates and `scripts/check_pages.py`, the nine agents and ten skills, the deny-list rebuild (D-017), `project/COVERAGE.md`, and edition 1.2.1 (access dates on every reference, seven older references corrected, access dates shown on the References page, the Gate entry reworded). The editorial reviewer returned REVISE; the fixes in `git diff f98a1dc 99d207e` were confirmed by the editorial and accuracy reviewers (D-011). After the confirmations, two non-content suggestions from the editorial confirmation were applied: STATUS records the v1.2.0 release, and the architect's agent file and skill name `project/plans/`.

Source fingerprint at acceptance: `cfa7e3b27e69eb5827e3b845d54ceea2e9aaec769f1feb295bf15df86226b87b`.

## Limitations

This session's network policy blocks martinfowler.com, openai.com, thoughtworks.com, wikipedia.org and dora.dev. Of the eight references dated in this edition, Spec Kit was verified by a direct read on github.com. The other seven (bockeler-harness, bockeler-sensors, bockeler-context, openai-harness, fowler-ci, fowler-fitness, ashby) were verified from search results: titles, bylines, dates and addresses. The bockeler-context note's examples and ashby's attribution to Böckeler rest on secondary summaries. "Accessed" therefore records the date these details were last checked, as the changelog says. A direct re-read is in STATUS Next.

## Confidentiality reviewer

**Scope.** `origin/main..4f11991` on `local/wave0` (51 files): agents (9) and skills (10), the removed `.github/agents`, prompt, instruction and workflow edits, `CLAUDE.md`, `README.md`, `content/{CHANGELOG.md,VERSION,glossary.yml,references.yml}`, `project/{AGENTS,COVERAGE,DECISIONS,STATUS}.md`, `scripts/{check_citations,check_pages,denylist}.py`, `scripts/outdated_terms.yml`, five test files, `site/generate.py` and six templates. No reference pages yet; reader-visible changes are the References page (access dates, five corrected entries), one glossary definition and the 1.2.1 changelog entry.

**Evidence.** `python scripts/check_citations.py`: "Citation check passed: 0 chapters, 7 series parts, 12 citations, 35 terms, 14 references." `python -m unittest discover -s scripts/tests`: 69 tests, OK. `denylist.matches()` run by hand over the full diff, the intermediate commit, the commit messages and every tracked file: 0 hits. A case-insensitive manual search for the protected names: no matches (none written here). The intermediate commit is a strict subset of the final state, so nothing was added and later removed. Every `COVERAGE.md` label checked against the public map: all 87 are map labels or close shortenings. `git status --ignored`: only `__pycache__` and generated `site/` output; no scratch, brief or inventory files.

**Checks performed.**
- GR-1.1 names: pass. The only counts added are about the map (55/31/1 of 87 boxes) and the deny-list (14 hashes); neither is an internal figure.
- GR-1.1 fingerprinting: pass. The coverage profile fits many organisations running SAFe on web or cloud products and points to no sector, stack or organisation; the tools named are generic categories already on the public map.
- GR-1.2 copying and general practice: pass. Agent and skill texts, templates, D-017 and `COVERAGE.md` use only public-map labels and the repository's own terms.
- GR-1.3: pass. No document, excerpt or note in the diff or ignored folders; briefs are directed to the scratchpad.
- GR-1.4: pass. No plain names in `denylist.py`, the tests (invented fixtures), D-017, STATUS or commit messages; categories and count only.
- A3 boundary: pass, with one mild addition (suggestion 3).
- GR-3.3: pass. Products appear only in reference titles and bylines already on the References page.
- D-015: pass. Only public SAFe terms the map already carries.

**Blocking issues.** None.

**Suggestions (non-blocking).**
1. GR-1.4: the salt is published beside the hashes, so a reader with a candidate name can confirm a guess. Consider an HMAC keyed by a CI secret (GR-1.4 allows a CI secret), with a local fallback. Earlier hashes stay in history; a fix limits future exposure only. For the author to decide.
2. GR-1.1: `check_denylist()` in the citation gate scans only `content/`. (The site tests scan every tracked file.) Consider scanning commit messages in the PR range in CI.
3. GR-1.2: the source-researcher checklist listed oddly specific code types; use generic phrasing. (Taken.)
4. GR-1.2: `COVERAGE.md` shows which future pages rest most on library experience; give reviews of "covered" boxes an extra close-paraphrase pass, and never add columns to the table.
5. Squash the "wip" commit before merge. (Done.)
6. STATUS refers to `content/reviews/1.2.1-wave0.md`, not yet in the diff. (Added with the review record.)

Reviewer verdict: ACCEPT

## Accuracy reviewer

**Scope.** Commit `4f11991`, `git diff origin/main 4f11991`; reader-visible content of edition 1.2.1: `content/references.yml`, the glossary `gate` entry, `content/CHANGELOG.md` 1.2.1, `content/VERSION`, and the access-date suffix in `site/generate.py`; `scripts/outdated_terms.yml` for correctness. Pages affected: References and Glossary (Gate). Read-only.

**Evidence.** `python scripts/check_citations.py`: "Citation check passed: 0 chapters, 7 series parts, 12 citations, 35 terms, 14 references." All 14 references now carry `accessed`; this commit adds it to bockeler-harness, bockeler-sensors, bockeler-context, openai-harness, fowler-ci, fowler-fitness, spec-kit and ashby. Network: dora.dev, martinfowler.com, openai.com, thoughtworks.com and wikipedia.org blocked; github.com and raw.githubusercontent.com reachable.

| Key | Checked | Method | Result |
|---|---|---|---|
| fowler-fitness | Title; Paula Paul and Rosemary Wang; Thoughtworks; 2019-01; note | Search only (Thoughtworks result title; summary gives authors and 11 January 2019; InfoQ 2019-02 reports the same) | Supported |
| openai-harness | Ryan Lopopolo; OpenAI; 2026-02; title | Search only (openai.com result title; several mirrors name Lopopolo, one gives 2026-02-11) | Supported |
| spec-kit | Title; commands include specify, clarify, plan, tasks, implement | Direct read on github.com (About line; README commands; `templates/commands/clarify.md` HTTP 200) | Supported; clarify exists as a command though the README quick-start leaves it out |
| bockeler-context | Date 2026-02; note; title | Search only (result title; Fowler's announcement decodes to 2026-02-05 and describes context configuration features) | Month supported; the note's examples rest on the announcement and secondary summaries |
| bockeler-sensors | Date 2026-05; sentence-case title | Search only (instalments from 2026-05-19; summaries say 27 May) | Supported; month precision suits an article in instalments |
| bockeler-harness | Sentence-case title | Search only | Supported |
| fowler-ci | "origin" reworded to "classic statement" | Search only (revised January 2024) | Supported; more accurate |
| ashby | Access date; "cited by Böckeler" | Search only (Wikipedia article exists; secondary summaries say the harness article invokes Ashby's law) | Supported; the URL anchor not confirmed |

Outdated terms, search only: SAFe 6.0 renamed Program Increment to Planning Interval and kept "PI" — correct. DORA now uses five metrics in throughput and instability groups — correct. DORA renamed "mean time to recover (MTTR)" / "time to restore service" to failed deployment recovery time in 2023 — correct. Regex tests: `four (?:DORA )?keys?` also matches "the four key roles" (false positive); the MTTR pattern misses "mean time to recover(y)".

**Checks performed.** GR-2.1 pass; GR-2.2 pass (the gate entry now credits "evolutionary-architecture practice", matching the cited article); GR-2.3 pass; GR-2.4 pass (earlier overclaims corrected: Spec Kit and Fowler CI as "the origin", the wrong fitness-function byline); GR-2.5 pass; terminology pass; citation gate pass; `site/generate.py` pass (escaped, shown only when present).

**Blocking issues.** None.

**Suggestions (non-blocking).**
1. GR-2.5 / GR-5.2: `accessed: "2026-09-25"` is honest as a "last checked" date, not as a record of a full read: seven of the eight newly dated references were verified from search results only, spec-kit by direct read. The review record should say so per reference, and note that the bockeler-context note and ashby's attribution rest on secondary summaries. Re-confirm from a session with open egress.
2. GR-5.3: extend the MTTR pattern to "mean time to recover(y)"; consider "change failure rate" → "change fail rate" and "lead time for changes" → "change lead time".
3. Narrow the "four key" pattern so "four key roles" does not fail the build.
4. GR-2.4: `outdated_terms.yml` sources could point at pages that state the change (a DORA metrics history page; a SAFe 6.0 "what's new" page). Not reader-visible.
5. GR-2.5: `fowler-ci` could record its revision date (2024-01).
6. CHANGELOG: "two Böckeler titles now match their published sentence case" is more exact than "use the site's sentence case".

Reviewer verdict: ACCEPT

## Accuracy reviewer, confirmation (`git diff f98a1dc 99d207e -- content/ scripts/outdated_terms.yml`)

**Evidence.** `python scripts/check_citations.py`: "Citation check passed: 0 chapters, 7 series parts, 12 citations, 35 terms, 14 references."

**Checks.**
- CHANGELOG 1.2.1 (GR-2.4, GR-2.5): the count of seven is right (fowler-fitness, openai-harness, spec-kit, bockeler-context, bockeler-sensors, bockeler-harness, fowler-ci) and each description matches `references.yml`. It now says what "accessed" records (the date title, author, date and address were last checked), which is honest for the references checked by search only. "Match their published sentence case" resolves suggestion 6.
- fowler-ci `date: "2024-01"`: Fowler's January 2024 revision, confirmed earlier by search only (his announcement post). Resolves suggestion 5.
- `outdated_terms.yml`: tested locally. "four keys", "four key metrics" and "four DORA metrics" are flagged; "the four key roles" is not. "time to restore service" and "mean time to recover(y)" are flagged. "change failure rate" is flagged, with "change fail rate" as DORA's current name (confirmed earlier by search only). The current terms are not flagged.

**Blocking issues.** None.

**Suggestions.** The bare acronym MTTR is no longer flagged, which is acceptable given its uses outside DORA; reviewers should watch for it used as a DORA metric. `dora-metrics` still points at `dora.dev/research/` rather than a page that states the metric changes. The record must state each reference's verification method (it does, under Limitations).

Reviewer verdict: ACCEPT

## Editorial and tooling reviewer, round 1

**Scope.** All 51 files of round 1. No reference pages yet. Brief: (a) the access-date check and outdated-terms list are present; (b) the six templates and the structure gate are present; (c) the nine agents and skills, plus `section-pipeline`; (d) `project/COVERAGE.md`, with no inventory file in the diff.

**Evidence.** `check_pages.py`: "Page check passed: 0 pages against 6 templates." `check_citations.py`: passed. Unit tests: 69 OK. `site/generate.py`: 4 pages, 2 diagrams. `release_content.py check`: stale, as expected before this record. Template headings match the APPROACH.md table exactly; coverage totals 55/31/1 match the rows.

**Checks performed.** GR-3 voice in reader-visible text: pass. GR-4.1 templates: pass, and the gate is in CI, CLAUDE.md and README. Agent and skill files: valid front matter, names match the roster, reviewers have no Edit or Write tool, reviewer skills end with a "Reviewer verdict" line. D-014 to D-017 agree with the diff, apart from the blocking issues. GR-2.5: all 14 references complete.

**Blocking issues.**
- B1 (GR-1.3, GR-5.1): the AGENTS.md roster sent page briefs to `briefs/<page>.md` in the repository, contradicting the agents.
- B2 (GR-5.1, GR-4.1): `check_pages.py` fails a page whose review record does not exist, which the pipeline writes only after review, so no new page could pass its own checks.
- B3 (GR-5.1): agents "hand off" to other agents, which subagents cannot do; nothing said the main session orchestrates.
- B4: the 1.2.1 changelog said five corrected references where seven changed, and "checked against the sources" overstated search-only checks.
- B5: STATUS was not accurate (session log, a missing review file, Next items, stale bullets).

**Suggestions.** S1 outdated-term patterns match ordinary text; S2 "v0.x" not caught, "draft" in titles, summaries, headings and link text not caught, and the narrowed reading of GR-3.2 should go to the author; S3 per-section word minimums, link targets counted as words, headings inside code fences; S4 require author or organisation, use UTC; S5 extend `check_citations.py` and the generator to pages before wave 1; S6 word-pair separator; S7 two weak tests; S8 consistency between roster, agents and skills (who writes references, palette, the editorial agent's commands, README, D-017 and ordinary-word identifiers, COVERAGE labels); S9 the Gate entry's credit and "this series" in notes; S10 template instructions left in copied pages.

Reviewer verdict: REVISE

## Editorial and tooling reviewer, round 2 (confirmation of `git diff f98a1dc 99d207e`)

**Evidence.** `check_pages.py` passed (0 pages, 6 templates); `check_citations.py` passed; 74 unit tests OK; `site/generate.py` generated 4 pages and 2 diagrams. No template keeps its instructions in the front matter.

**Checks.** B1 fixed (plans in `project/plans/`, briefs and inventory scratchpad-only, an Orchestration section). B2 fixed (the site-builder creates the record with Scope and Limitations before the first check; documented in the agent, its skill, `section-pipeline` and the `check_pages.py` docstring). B3 fixed (the main session orchestrates; agents return their outputs; a test guards the wording). B4 fixed (all seven references listed; "accessed" defined; a direct re-check in Next). B5 fixed (session log, Next, stale bullets, a "For the author" list raising the GR-3.2 reading under GR-6.2). S1 to S8 and S10 done, with tests for S1, S3 and S6; COVERAGE labels and S9 deferred. GR-3 voice in the new changelog lines: pass.

**Blocking issues.** None.

**Suggestions.** 1 STATUS still said 1.2.0 was not yet tagged (applied). 2 Name `project/plans/` in the architect's agent file and skill (applied). 3 The References page shows "accessed" for search-only checks; Next item 4 covers the re-check. 4 `\s+` joins words across blank lines, which errs on the safe side.

Reviewer verdict: ACCEPT

## Result

Confidentiality ACCEPT, accuracy ACCEPT (with confirmation), editorial ACCEPT (round 2). No open blocking findings.

Verdict: ACCEPT
