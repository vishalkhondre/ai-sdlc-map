# Review: Core section (edition 1.4.0, D-018)

## Scope

The Core band of the map (section plan `project/plans/core.md`), reviewed against base commit `4f82327`. Nine reference pages in `content/pages/core/`: harness engineering, the Engineering Kit, its five parts (rule registry, validators, skills with evals, evidence schema, adapters), workflows and the software factory. With them: the terminology entries they add or change in `content/glossary.yml`, the references they add or correct in `content/references.yml`, the map changes (the kit's five parts as separate labels, and the core labels linked to these pages in `content/diagrams/map/`), the routing row, the reading order in `content/toc.yml`, and the 1.4.0 entry in `content/CHANGELOG.md`.

## Limitations

The research session's network policy blocked most primary sources, so many references were verified from search results only rather than by reading the source. The list of those references, with what each needs from a direct read, is kept in the research notes outside the repository, not here.

Three independent reviews by reviewer agents in fresh contexts (`.claude/agents/`), read-only, in three rounds (D-011). Between rounds 1 and 2, a separate check of the draft pages against the private source library, run by the source researcher because the confidentiality reviewer never sees the library (AGENTS.md, Independence), led to every page being revised and several passages being cut and written again without access to the library or to earlier wording (GR-1.2). The section lands as one commit on top of `4f82327`.

## Confidentiality reviewer

### Round 1

**Evidence.** `check_citations.py` passed (9 pages, 140 citations, 45 terms, 109 references); 97 unit tests OK; the deny-list matcher and the D-019 patterns over every changed file, the built site, the diff and the commit messages: no hits.

**Checks performed.** GR-1.1 names and fingerprinting combinations: pass (generic roles and cadences; every figure has a public citation; the running-example identifiers were already public). GR-1.2: no finding from reading cold, with five passages and one repeated pattern raised for the author to confirm against the library. GR-1.3, GR-1.4, D-019: pass.

**Blocking issues.** GR-3.3: a model vendor was named in page prose on the validators page.

Reviewer verdict: REVISE

### Round 2

The vendor naming was fixed and every page re-read cold: no GR-1.1, GR-1.2 or GR-3.3 finding in the pages. Blocking: GR-1.2, superseded draft text remained in the working branch's history and on a remote branch; GR-1.1/GR-1.4, the status file named wording and a commit it should not.

Reviewer verdict: BLOCK

### Round 3

The status file was fixed, and the section was rebuilt as a single commit whose only parent is `4f82327`, with none of the earlier drafts in its ancestry; the deny-list and D-019 scans over that tree, the built site and the commit message: no hits. GR-1.1 to GR-1.4, GR-3.3 and D-019 pass for the tree that would land. Open: the superseded draft is still reachable from a remote working branch; removing it needs the author (delete or reset the branch; ask for unreferenced commits to be purged if the repository is public).

Reviewer verdict: BLOCK

### Confirmation (author's answers, D-025 and D-026)

The author's answers and the process rule checked for GR-1.1 to GR-1.4, GR-3.3 and D-019: the
new decision and agent wording is generic and names nothing; one sentence that tied reworded
wording to its reason was made neutral. Deny-list and D-019 scans over the changed files, the built
site and the commit messages: no hits. Open: the superseded draft on a remote working branch.

Reviewer verdict: BLOCK

### Confirmation (remote branch removed)

No head, pull-request head or tag on the remote contains the superseded draft, and its branch no
longer exists. The section lands as one commit on `4f82327` with a neutral message; the review
record's confirmation entries name nothing. Purging unreferenced commits from the host is with the
author.

Reviewer verdict: ACCEPT

## Accuracy reviewer

### Round 1

**Evidence.** Every reference URL probed; 49 new references read directly (the published page or the publisher's own source), 46 checked from search results only because the network blocked the host. A claim → source → supported? table for all nine pages.

**Blocking issues.** GR-2.2/GR-2.4: the harness page credited the term itself to Böckeler, whose writing says it was already in use. GR-2.4/GR-2.3: the skills page said a vendor's guidance "records" a failure it gives as an example. GR-2.5: three reference titles or bylines not as published (`owasp-asvs-v4-users`, `homebrew-doctor`, `dora-vsm-guide`).

Reviewer verdict: REVISE

### Round 2

All five fixed. Every rewritten passage re-checked against its source; new citations (least privilege and separation of duties in SP 800-53, read directly; the flaky-test, policy-engine, change-host, test-framework, SLSA and SARIF sources in new places) say what the pages claim; unsourced points are presented as recommended practice; the changelog is accurate (94 new references). The search-only references remain to be read directly before the release tag.

Reviewer verdict: ACCEPT

### Confirmation (`b7f8e4e..57cff99`)

Two reference notes reworded and editorial wording edits: no claim changed its support; the changelog still holds.

Reviewer verdict: ACCEPT

### Confirmation (author's answers, D-025)

The reference key renamed to fitness-function, Adapter credited as adapted from Cockburn, the golden
path credited to Spotify Engineering, the kit versioning policy, two archived Radar entries labelled
with their volume, and the reworded catalog, glossary and adoption-path lines: every claim keeps its
support, and the changelog lines are accurate.

Reviewer verdict: ACCEPT

## Editorial and code reviewer

### Round 1

**Evidence.** Page and citation gates, build, diagram render, 97 unit tests and browser checks pass; screenshots in light and dark at 1400 and 390 px with no overflow.

**Blocking issues.** The running example's facts disagreed between pages; the harness page claimed a map link that does not exist; the factory was called an operating model, the name of another map box; mechanism and meaning were reversed on the evidence schema page; the adapters page set what a result means for a merge, which the plan gives to assurance.

Reviewer verdict: REVISE

### Round 2

All five resolved with one timeline and shared wording on every page; suggestions applied. Blocking: the validators pitfall relied on a term the page no longer defined after a cut.

Reviewer verdict: REVISE

### Round 3

Resolved, and the remaining consistency suggestions (the incident export within R-017's limit, exception marking in the registry, merge meaning left to assurance, glossary alignment) applied.

Reviewer verdict: ACCEPT

### Confirmation (author's answers, D-025)

The Feedback path proven label opens the harness page's steering-loop section; the map's credit line
names Cockburn without crowding; the adoption path re-flows cleanly in both themes and at both
widths; the versioning paragraph states one rule (clarified to the kit's defaults); the process rule
is clear. Build, diagrams, 97 unit tests and browser checks pass.

Reviewer verdict: ACCEPT

## Result

Confidentiality ACCEPT (confirmation after round 3), accuracy ACCEPT, editorial and code ACCEPT. No
open blocking findings.

Verdict: ACCEPT
